#include <iostream>
#include <cstdio>

using namespace std;

int main(){
	double ax, ay, bx, by, cx, cy, dx, dy;
	while( scanf(" %lf %lf %lf %lf %lf %lf %lf %lf", &ax, &ay, &bx, &by, &cx, &cy, &dx, &dy) != EOF ){
		bool flag=false;
		
		//枠の外にある頂点が１つ，中にある頂点が１つあれば良い
		
		
				
		if(flag) printf("YES\n");
		else printf("NO\n");
	
	}


	return 0;
}