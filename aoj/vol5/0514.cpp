#include <cstdio>
#include <iostream>
using namespace std;

int main(){
	int a, b, c, n;
	int parts[300];
	
	for(int i=0; i<300; ++i) parts[i]=2;
	
	scanf(" %d %d %d", &a, &b, &c);
	if(a==0 && b==0 && c==0) break;
	scanf(" %d", &n);
	
	for(int i=0; i<n;++i){
		
		
		
		
	}
	
	return 0;
}