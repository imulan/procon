#include <cstdio>
#include <iostream>
using namespace std;

int main(){
	long w, h, x[30], y[30];
	int n;
	
	scanf(" %ld %ld %d", &w, &h, &n);
	for(int i=0; i<n; ++i) scanf(" %d %d", &x[i], &y[i]); 	
		
	
	
	return 0;
}