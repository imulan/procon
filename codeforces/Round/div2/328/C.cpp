#include <cstdio>
#include <iostream>
#include <utility>
using namespace std;

typedef long long ll;

ll gcd(ll p, ll q){ //p>=q
	if(p%q==0) return q;
	else return gcd(q,p%q);
}

ll lcm

int main(){
	ll t,w,b;
	cin >> t >> w >> b;
	
	
}